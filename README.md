#Part II Programming Exercise 3

Required libraries
Four libraries are required for this program to run, three of which are standard:

numpy
time 
matplotlib

A fourth non-standard library is also required:

numba

Numba speeds up the code by compiling Python functions into highly optimized machine code using Just-In-Time (JIT) compilation, eliminating interpreter overhead and enabling fast, vectorized numerical operations.
